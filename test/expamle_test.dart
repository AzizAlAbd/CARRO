import 'package:flutter_test/flutter_test.dart';

void main() {
  setUp(() => null);
  tearDown(() => null);
  group('this is a gorub of tests', () {
    test('test name', () {
      expect(5, 5);
    });
  });
}
